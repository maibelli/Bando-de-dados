CREATE DATABASE ESCOLA;
USE ESCOLA;
```

Criar Tabelas
*Tabela ALUNO*
```
CREATE TABLE ALUNO (
  ID_ALUNO INT PRIMARY KEY,
  NOME VARCHAR(100) NOT NULL,
  EMAIL VARCHAR(100) UNIQUE NOT NULL,
  ENDERECO VARCHAR(200) NOT NULL
);
```

*Tabela PROFESSOR*
```
CREATE TABLE PROFESSOR (
  ID_PROFESSOR INT PRIMARY KEY,
  NOME VARCHAR(100) NOT NULL,
  EMAIL VARCHAR(100) UNIQUE NOT NULL,
  DISCIPLINA VARCHAR(50) NOT NULL
);
```

*Tabela TURMA*
```
CREATE TABLE TURMA (
  ID_TURMA INT PRIMARY KEY,
  NOME_TURMA VARCHAR(50) NOT NULL,
  ID_PROFESSOR INT,
  FOREIGN KEY (ID_PROFESSOR) REFERENCES PROFESSOR(ID_PROFESSOR)
);
```

*Tabela MATRICULA*
```
CREATE TABLE MATRICULA (
  ID_MATRICULA INT PRIMARY KEY,
  ID_ALUNO INT,
  ID_TURMA INT,
  FOREIGN KEY (ID_ALUNO) REFERENCES ALUNO(ID_ALUNO),
  FOREIGN KEY (ID_TURMA) REFERENCES TURMA(ID_TURMA)
);
```

Inserir Dados
*Tabela ALUNO*
```
INSERT INTO ALUNO (ID_ALUNO, NOME, EMAIL, ENDERECO)
VALUES 
(1, 'João Silva', 'joao@email.com', 'Rua 1'),
(2, 'Maria Souza', 'maria@email.com', 'Rua 2'),
(3, 'Pedro Oliveira', 'pedro@email.com', 'Rua 3');
```

*Tabela PROFESSOR*
```
INSERT INTO PROFESSOR (ID_PROFESSOR, NOME, EMAIL, DISCIPLINA)
VALUES 
(1, 'Prof. João', 'prof_joao@email.com', 'Matemática'),
(2, 'Prof. Maria', 'prof_maria@email.com', 'Português'),
(3, 'Prof. Pedro', 'prof_pedro@email.com', 'História');
```

*Tabela TURMA*
```
INSERT INTO TURMA (ID_TURMA, NOME_TURMA, ID_PROFESSOR)
VALUES 
(1, 'Turma A', 1),
(2, 'Turma B', 2),
(3, 'Turma C', 3);
```

*Tabela MATRICULA*
```
INSERT INTO MATRICULA (ID_MATRICULA, ID_ALUNO, ID_TURMA)
VALUES 
(1, 1, 1),
(2, 2, 2),
(3, 3, 3);
```

Consultas com Joins
*Alunos e suas turmas*
```
SELECT ALUNO.NOME, TURMA.NOME_TURMA
FROM ALUNO
INNER JOIN MATRICULA ON ALUNO.ID_ALUNO = MATRICULA.ID_ALUNO
INNER JOIN TURMA ON MATRICULA.ID_TURMA = TURMA.ID_TURMA;
```

*Professores e suas turmas*
```
SELECT PROFESSOR.NOME, TURMA.NOME_TURMA
FROM PROFESSOR
INNER JOIN TURMA ON PROFESSOR.ID_PROFESSOR = TURMA.ID_PROFESSOR;
```

*Alunos, turmas e professores*
```
SELECT ALUNO.NOME, TURMA.NOME_TURMA, PROFESSOR.NOME
FROM ALUNO
INNER JOIN MATRICULA ON ALUNO.ID_ALUNO = MATRICULA.ID_ALUNO
INNER JOIN TURMA ON MATRICULA.ID_TURMA = TURMA.ID_TURMA
INNER JOIN PROFESSOR ON TURMA.ID_PROFESSOR = PROFESSOR.ID_PROFESSOR;
```
